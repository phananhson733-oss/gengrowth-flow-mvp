# SEO supervisor report — 2026-06-30 (UTC+8)

## 20:05 CST — scheduled run (`auto-seo-blog`), incremental pass

Second stateless supervisor pass today, ~1h after the 19:05 run. Read-only live detection
from the Cowork Linux sandbox + `_staging` inspection. **Net change vs 19:05: the author lane
advanced one slug (PG-CELEB-010 authored); the publish gap on PG-CELEB-009 is unchanged, so
this run wrote a `publish-pending` kick to the bridge.**

### 1. Live state — both sites still GREEN, unchanged since 06-29

- **astrologywiki.com** — per-route `web_fetch` verification (SPA resolves per route):
  control `/en/wiki/mo-salah-birth-chart` returns full article meta + body (live);
  `/en/wiki/serena-williams-birth-chart` returns the **homepage shell** (`canonical: /`) →
  confirmed **soft-404 / not published**. Counts consistent with the 06-29 snapshot
  (260 EN + 126 ZH `/wiki/`). Zero new live slugs since 2026-06-29.
- **gengrowth.ai** — unchanged at 41 EN blog posts. Full W25 batch remains live.
- **Drive SSOT** — workbook `astrologywiki.com` reachable (read-only).

### 2. Author lane is healthy and advancing (Mac launchd timers)

`_staging` shows the Mac author timer is producing on schedule:

- **PG-CELEB-009** `serena-williams-birth-chart` — authored 11:58Z, `phase2.overall=pass`
  (1 H1 / 11 H2, 1725 words, 6 wikilinks, all RL1–RL6 green). EN md + manifest ready.
  **Publish-ready, not yet live.**
- **PG-CELEB-010** `jannik-sinner` — orchestrator started/finished **12:01Z** (just now),
  Claude draft ok, ~$0.056. Next in the author→QA→publish queue (not yet QA'd/published).

### 3. Action taken — `publish-pending` kick written

Because PG-CELEB-009 is publish-ready but soft-404, and the gap is publish (not authoring),
wrote `.gg-bridge/requests/kick-2026-06-30-2005.json` (`action: publish-pending`,
pendingSlugs=`[serena-williams-birth-chart]`). The Mac watcher should kick the publish lane
to push it live and run the tail reconcile.

> ⚠️ **`acks/` is still empty (0 acks ever).** The author launchd timer clearly runs, but no
> bridge ack has ever been written, so it's unconfirmed whether the `com.gengrowth.cowork-bridge`
> watcher + publish lane are installed/firing. If serena does not go live shortly, this is the
> likely human blocker — verify the watcher + publish timer on the Mac.

### 4. Sheet / GSC / Feishu sync — credentialed lanes, deferred to Mac (unchanged)

- **选题登记表 status-drift**: the 13 verified flips + 2 review rows remain staged at
  `reconcile/2026-06-30-status-drift.json`; apply via `gg-reconcile-status.mjs --apply` on the
  credentialed Mac lane (sandbox holds no sheet-write SA). No new drift detected this pass.
- **GSC『请求编入索引』**: `gsc/submitted-2026-06-30.json` stays `deferred-not-performed` —
  no newly-live slugs since 06-29, candidate set unchanged. Submission is the Mac GSC flow (~10/day).
- **Feishu notify**: no Feishu connector in this sandbox; notification is part of the Mac
  publish/notify lane and fires on actual publish, not from this read-only pass.

### Verdict

System healthy and advancing. Author lane on track (2 celeb drafts today). One publish-ready
draft (serena) is the only live gap; a `publish-pending` kick was issued. The only standing
risk is the never-acked bridge watcher — flagged for human confirmation. No side-effecting
writes to live sheet/GSC/sites were made from the sandbox.

---

## 19:05 CST — scheduled run (`auto-seo-blog`) + live operator request (wzb)

Stateless supervisor pass from the Cowork Linux sandbox. Read-only live detection on both
sites + Drive SSOT read. This run also actioned an explicit operator request from wzb about
`选题登记表` status drift (see §3). **No external side-effecting writes to the live sheet
were performed from the sandbox** (the sandbox has no sheet-write credential — that SA lives
on the Mac); the fix is fully computed, staged in-repo, and a reusable automation script was
added so the credentialed lane self-heals it going forward.

### 1. Result: both sites GREEN — publish lane current, nothing newly live since 06-29

- **astrologywiki.com (W22)** — sitemap crawl: **260 EN `/wiki/` + 126 ZH** live. Newest
  article lastmod **2026-06-29** (`matheus-cunha-birth-chart`, `mo-salah-birth-chart`,
  `prince-william-birth-chart`, `solar-return-chart`). **Zero new slugs since the 06-29
  snapshot.** The Mac author/publish timers produced PG-CELEB-009 (`serena-williams-birth-chart`)
  as a fresh `_staging` draft **today 10:54Z** — authored, not yet published (correctly still
  pending).
- **gengrowth.ai (W25)** — sitemap crawl: **41 EN blog posts** live (the 4 `/category/*`
  index pages carry a 2026-06-30 regenerated timestamp but are not articles). Identical to
  the 06-29 count; full W25 B2B/SEO batch remains published.
- **Drive SSOT** read OK — workbook `astrologywiki.com`
  (`1CkjOCgYbRfXGYc6l2FJOaxUIzxT0NBVUhUpgCjyzcQc`, modified today 11:03 CST).
- **Publish backlog = 0** beyond the one authored-but-unpublished draft above. No stuck
  preview.

### 2. GSC『请求编入索引』— no change

No newly-live slugs since 06-29, so the index-request candidate set is unchanged (the same
25 EN deltas already recorded in `gsc/submitted-2026-06-29.json`, `status: deferred`). Actual
Search Console submission is an external write outside this task's scope; left to the Mac GSC
flow (~10/day cap).

### 3. NEW — `选题登记表` status-drift reconciliation (operator request)

wzb flagged that many `选题登记表` rows sit at『待写』/『写作中』although the article is
already published, and asked that this be (a) corrected and (b) folded into the automation.

Investigated the full registry (204 rows: **164 已发布 / 37 待写 / 3 写作中**) and crossed
every open-status row against the live `/en/wiki/` slug set. **13 rows are confirmed live but
mislabeled** — the publish stamp was missed. Full machine-readable detail (page_id-keyed, with
slug + live URL + verified sheet row) is staged at
`.gg-bridge/reconcile/2026-06-30-status-drift.json`.

| page_id | now | → | live slug | sheet row |
|---|---|---|---|---|
| PG-AURA-009 | 待写 | 已发布 | aura-reading | (upper) |
| PG-NAKSH-001 | 写作中 | 已发布 | nakshatra | (upper) |
| PG-NAKSH-004 | 写作中 | 已发布 | pushya-nakshatra | (upper) |
| PG-AI-001 | 待写 | 已发布 | ai-astrology-app | (upper) |
| PG-WC-037 | 待写 | 已发布 | folarin-balogun-birth-chart | 203 |
| PG-WC-038 | 待写 | 已发布 | darwin-nunez-zodiac-sign | 204 |
| PG-COUPLE-001 | 待写 | 已发布 | jwoww-zack-carpinello-wedding-synastry | 205 |
| PG-COUPLE-002 | 待写 | 已发布 | reese-witherspoon-oliver-haarmann-compatibility-astrology | 206 |
| PG-CELEB-006 | 待写 | 已发布 | ricky-gervais-zodiac-sign | 207 |
| PG-CELEB-007 | 待写 | 已发布 | taylor-swift-and-travis-kelce | 209 |
| PG-CELEB-008 | 待写 | 已发布 | harry-styles-and-zo-kravitz | 210 |
| PG-WC-039 | 待写 | 已发布 | jordan-vs-argentina | 211 |
| PG-WC-040 | 待写 | 已发布 | colombia-vs-portugal | 212 |

**Needs human review (2):** PG-TERM-008 (`midheaven`) and PG-TERM-009 (`opposition`) — both
slugs are live, but as core glossary pages dated 2026-06-01; ambiguous whether the card equals
the live page or is a planned rewrite. Not auto-flipped.

**Correctly still pending:** PG-CELEB-009 (serena, authored today, awaiting publish), plus
PG-CELEB-010/011/012 (sinner / teyana taylor / ben shelton), PG-WC-041 (morocco), and the
older un-authored backlog (VEDIC, EMPATH, TERM-007/010/011, MYTH-003/004/005, etc.) — verified
not live.

### 4. Automation added — `tools/scripts/gg-reconcile-status.mjs`

New reusable script that makes this self-healing (wzb's "加入自动化流程" ask). It fetches the
live sitemap for each product, reads `选题登记表`, and for any open-status row whose
`.gg-cache/<page_id>` entity slug is live, flips Status→『已发布』and backfills 发布URL
(idempotent; never touches closed statuses; keyed on page_id, not row position). Exact/diacritic
matches auto-apply; fuzzy token matches are emitted for review only.

- Dry run: `node tools/scripts/gg-reconcile-status.mjs`
- Apply:   `node tools/scripts/gg-reconcile-status.mjs --apply`

Recommended wiring: call it (apply mode) at the **tail of the publish lane** and once per
supervisor cycle on the Mac, where `GG_WRITER_SA_JSON` + the workbook IDs are present.

### Why this run did not itself write the sheet

Unchanged from the canonical architecture: the Cowork sandbox can read the live sites + Drive
but holds **no sheet-write credential** (the writer SA lives on the Mac), and editing a 200-row
SSOT by browser-canvas row position is unsafe (gviz collapses blank rows, so computed row
numbers drift — observed +1 above the 208 blank row). The fix is therefore computed and staged
(`reconcile/2026-06-30-status-drift.json`) for the credentialed `gg-reconcile-status.mjs` apply
pass; the 13 flips are page_id-keyed and verified, so application is deterministic.

### Verdict

System healthy. Both W22 and W25 are current with zero publish backlog (one draft authored
today awaiting publish). The substantive new finding is **status drift in `选题登记表`** —
13 published pages were still marked 待写/写作中; the correction is computed + staged and a
self-healing reconcile script was added so future runs fix this automatically. Outstanding
operator actions: run `gg-reconcile-status.mjs --apply` on the Mac (applies the 13 flips +
backfills URLs), and decide the 2 review rows (PG-TERM-008/009).
