# Runtime caches and staging — keep clean repo, regenerate as needed.
.gg-cache/
_staging/*.tmp-*
node_modules/
.DS_Store
*.log
.env
.env.local
# Service Account / secrets (NEVER commit)
_gg.env
*.sa.json
# Claude Code agent worktree leftovers (transient, per-agent dirs)
.claude/worktrees/
# Obsidian opens any dir as vault and dumps config here — not a real vault.
.obsidian/
