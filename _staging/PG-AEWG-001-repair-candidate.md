我没法用「加个 H1」的方式来通过这次 QA —— 因为那样做只是把上一轮**正确的拒绝**伪装成一篇合格文章，把问题从「没有 H1」偷偷换成「有 H1 的误导内容」。让我说清楚为什么，以及真正能立刻通过 phase2 的做法。

## phase2 报「no H1」其实是症状，不是病根

这份草稿没有 H1，不是因为漏排了一行标题，而是因为**上一轮根本没有生成文章正文**——它给出的是一段有理有据的拒绝说明。所以 phase2 找不到 H1 是必然的：没有正文，就没有 H1 可挂。

给这段拒绝文字（或者硬生成的 SaaS 模板文）补一个 `#` 标题，只会让下面这些问题带着一个合格的 H1 溜过自动 QA：

- **它会对一位真实、在世、且年轻的运动员（Alex Eala）编造失实框架**——把一个人的温网历程说成「可白标转售的方案」「可分步实施的交付物」「Baseline vs White-label/SaaS 两种做法」。§3 / §5 / §7 按模板要求写出来，**每一句在事实层面都是假的**，不是风格问题，是构造性造假。
- `search_volume: 0` + `track: 量产线` 说明它服务的读者数是零，纯粹是索引灌水页。

「补 H1 让它过 QA」恰好是把这两个问题一起放行。这我不做。

## 能立刻合法通过 phase2 的三条路（任选其一，我马上开工）

1. **写一篇真实的 Alex Eala 温网成长故事**——正常体育人物稿，带正规 H1/H2，严格限定在公开可核查的比赛与采访范围内，**去掉 white-label / SaaS / 定价 / 分步实施那套框架**。这样 phase2 的 no-H1 会被真正修好，而不是被绕过。

2. **保留 GenGrowth 那套 11-section 模板，换一个模板真正适配、且有搜索需求的实体**（white-label SEO、content gap analysis、SEO reseller pricing 之类）。每一节都言之有物，H1 自然就有了。

3. **当成 pipeline 的实体映射 bug 处理**：一个 SaaS 采购模板被错误分配给了体育实体 `PG-AEWG-001`。这种情况正确的动作是在队列层面把这个 claim 打回 / 修正映射，而不是强行生成一篇 Frankenstein 稿骗过 QA。

告诉我走哪条。如果只是想让这条 claim 别再卡在 phase2，我推荐 **第 1 条**——它是唯一既修好 no-H1、又不产出关于真实人物的失实内容的做法，我可以直接把整篇写出来。
