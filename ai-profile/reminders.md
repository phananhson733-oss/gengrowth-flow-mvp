---
title: 跨会话待办提醒
type: reminders
updated: 2026-06-18
---

# 跨会话待办提醒（Reminders）

> 会话开始时若有 `- [ ]` 未完成条目，在第一条回复前列出（见 AGENTS.md §七）。

## 待完成

- [ ] 2026-08-25 13:15 | **P1 · 昨天(08-24)有人把 `seo-blog` 和 `seo-reconcile` 两条 plist 重命名停用了；astrologywiki 发布链已彻底静止** —— 由 GSC 每日 cron 只读诊断查出，未擅自动手。
  - **新增证据**：`~/Library/LaunchAgents/com.gengrowth.seo-blog.plist.disabled-20260824`、`com.gengrowth.seo-reconcile.plist.disabled-20260824`（后缀日期=昨天；文件 mtime 分别是 7/13、7/16，即只改了名没改内容）。这动作看着是人为的，**需 wzb 确认是有意为之还是排障留下的**。
  - **连带现状**：`launchctl list` 里 seo-* 一条不剩；`print-disabled` 显示 `seo-nightly / seo-blog / seo-author-kicker / seo-autopilot / seo-author / gengrowth-author / recap-performance` 全 disabled。lane_watchdog 每 30min 报 5/7 violating：`seo-author` age=1065h、`seo-autopilot` age=1064h（≈44 天，末次日志 2026-07-12）、`gengrowth-author`/`seo-nightly` age≈283h。
  - **对 SEO 的实际影响**：`sync-published` 的 `en_urls` 从 **2026-07-30 到 08-25 连续 27 天恒为 361**，一篇新文都没上线。所以今天 GSC 待提交候选=0 **不是好事**——是上游没产出，不是全收录了。url-inventory 侧 rows=421 / indexed=309 / untracked=112 也 4 天没动。
  - **建议**：先确认停用意图；若要恢复，`mv` 回 `.plist` + `launchctl enable` + `bootstrap`，⚠️按 08-13 教训改完复验 `gengrowth-publish`/`ledger-reconcile`/`flow-driver` 这三条本来在跑的 lane 还在不在。

- [ ] 2026-08-23 13:15 | **P1 · `com.gengrowth.index-monitor` 每日 cron 今天起停跑（首次缺勤）** —— 由 GSC 每日 cron 只读诊断查出，未擅自动手 launchctl。
  - **现象**：`~/gengrowth-agents/cron-sync/index_monitor/` 里 08-17→08-22 每天都有日志（固定 09:01–09:02 启动，08-22 以 `index monitor ok` 正常收尾），**08-23 无日志文件**；13:10 复查仍无，缺勤 >4h，不是延迟。
  - **launchd 侧**：`launchctl print gui/501/com.gengrowth.index-monitor` 与 `user/501/...` 均 `Could not find service`；`launchctl print-disabled gui/501` 里它是 **disabled**（与 `seo-blog`/`gengrowth-author` 同列）。plist **只在 repo `tools/scripts/com.gengrowth.index-monitor.plist`**，从未装进 `~/Library/LaunchAgents`（该目录 mtime 仍 07-22）。无重启（uptime 5 天，末次 boot 08-17 22:59）。
  - **watchdog 已在报但不修**：`lane_watchdog` 每 30min 一条 `index-monitor not-loaded age=28.0h`，已超 `lanes-manifest.mjs` 里的 `maxGapSec=26h`；`GG_WATCHDOG_AUTOHEAL` 未开 → 只读诊断。
  - **影响**：两站的 url-inventory / index-tracking / 结果复盘表 / request-queue 每日自动回填 + sitemap 每日 Sitemaps API 刷新全停。
  - **今日已人工兜底**（astrologywiki 腿）：`--sync-published/--sync-url-inventory/--check-due/--sync-recap/--sync-request-queue`，数字与 08-22 一致（en_urls=361、rows=421 untracked=112 indexed=309、due=0、queue rows=0）。gengrowth 腿未补（其 `en_urls=0` 是上面 08-21 那条 P1 的已知根因）。
  - **建议**：`launchctl enable gui/$(id -u)/com.gengrowth.index-monitor` 后 `bootstrap` repo 那份 plist；⚠️ 参照 08-13 前车之鉴（修保活 lane 时把授稿 lane 弄没了），改完必须复验原本在跑的 lane 还在不在；另注意该 plist 含 `LimitLoadToSessionType=Background`。
  - **08-25 13:xx 复查（GSC 每日 cron）**：已自行恢复运行 —— 08-24 09:02、08-25 09:02 两天日志齐全且均以 `index monitor ok` 收尾（08-23 仍缺勤，只丢那一天）。但 launchd 侧**依旧没修**：`~/Library/LaunchAgents` 里无 `com.gengrowth.index-monitor.plist`、`print-disabled` 仍是 `disabled`，说明它不是被 launchd 拉起的（另有调度源）。lane_watchdog 因此继续报 `index-monitor not-loaded`，但 age 只有 4.0h（跟着实际日志走）→ **该告警现在是 launchd 注册缺失的误报，不是真停跑**。要么补 plist、要么把 watchdog 的判据从「launchd 已加载」改成「日志新鲜度」，二选一。

- [ ] 2026-08-21 13:15 | **P1 · gengrowth.ai 索引监控静默失效 23 天，且 cron 全程报 `ok`（fail-open）** —— 由 GSC 每日 cron 只读诊断查出，未擅自动手。
  - **现象**：`index_monitor` 里 `product=gengrowth` 的 `sync-published` 自 **2026-07-29 起 `en_urls=0`**（07-21→07-28 稳定 62~64，07-31 有过一次 12 的回光），连带 `sync-url-inventory rows=0` → gengrowth 的 url-inventory / 收录跟踪表这 23 天全是空转，且每天日志都写 `index monitor ok`，从未告警。
  - **时间点强相关**：07-28 gengrowth 发布迁移到 Nevermore，次日归零。
  - **根因**：`tools/scripts/gg-index-monitor.mjs` 的 `isEnWikiArticleUrl()`（~413 行）只认 `/en/wiki/` 与 `/en/blog/` 形状；迁移后 gengrowth.ai sitemap 共 **170 条 URL、带 `/en/` 的 0 条**（实际是 `https://gengrowth.ai/agents`、`/resources`、`/prompts` 这类裸路径）。
  - ⚠️ **是坑但不是根因，别只修这个**：配置里硬编码的 `https://www.gengrowth.ai/sitemap.xml` 现已 **301 → no-www**（默认值散落 4 处：`gg-index-monitor-tick.sh:70`、`gg-index-repair-resubmit-tick.sh:56`、`lib/flow-backfill.mjs:30`、`gg-reconcile-status.mjs:58`，可用 `GG_GENGROWTH_SITEMAP_URL` 覆盖）。**实测把 `--sitemap-url` 换成正确的 no-www 后 `en_urls` 仍是 0**，证明路径过滤器才是根因。对照组 astrologywiki 的 www sitemap 正常（200 / 70KB / en_urls=361）。
  - **建议**：① 让 `isEnWikiArticleUrl` 按站点参数化（gengrowth 用裸路径判定）；② 把 `en_urls=0` 改成 fail-closed 告警，否则这类归零还会再静默 23 天。
- [ ] 2026-08-21 13:15 | **（判读提醒，非待办）今早 09:02 `index_monitor` 的 `rc=1` 已确认是瞬时抖动，不要去改 GSC 权限** —— astrologywiki 三条腿撞 Google API `503` + 两个 `404`，13:1x 逐条重跑全过（en_urls=361 / rows=421 / fixed=0）。该 cron 的飞书告警模板会自动附一句「常见原因是 GSC reader SA 权限不足」，**是模板猜测不是诊断**。确认 07-21→08-20 该 cron 均为 `ok`，仅 08-21 这一次失败。

- [ ] 2026-08-14 13:20 | **🚨 授稿仍 0 产出：08-13 的修复只解决第一层，查出两个新 blocker（`en_urls=361` 连续第 16 天）** —— 08-13 19:00 那次 tick 的 preflight 确实首次 `status:"ready"` 通过并进入 nightly（第一层已修好），但：
  - **① P0 回归 · 唯一授稿 lane `com.gengrowth.seo-blog` 现在【未加载】。** `launchctl print gui/$UID/com.gengrowth.seo-blog` → `Could not find service`，而 plist 文件在 `~/Library/LaunchAgents/` 且**不在** `launchctl print-disabled` 里 → 属**纯未 bootstrap**（与 08-13 修的那三个 disabled 态不是一回事，**不要 `enable`，直接 bootstrap**）：`launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.gengrowth.seo-blog.plist`。**诱因推测=08-13 晚给三个保活 plist 做 enable/bootstrap 时把它顺带 bootout 了**——证据是那次 nightly 跑到第 4 篇 `PG-FD-002` 时被 `Terminated: 15` 腰斩，日志停在 `starting deterministic SEO nightly` 而无 `tick complete`。教训：改 launchd 后必须复验本来在跑的 lane 还在不在。
  - **② 第二层根因 · 即使 lane 活着也每篇必 park。** 08-13 实跑的 4 篇（`PG-FA-001`/`PG-FA-002`/`PG-FD-001`/`PG-FD-002`）全部 `PARK(author) … render produced no v8 prompt`。手动复现拿到准确原因（只读，可照抄）：`node tools/scripts/gg-render-batch.mjs --batch .gg-cache/batches/PG-FA-001.json` → `skipped — missing cfg fields: cluster_jtbd, internal_link_rule, cta_text, tier_gate_block, rl6_hint, friction_themes (add to overrides.json[PG-FA-001])`（同样信息也在 batch fixture 的 `todo` 数组里）。即这批 anime 系列新 PID 在选题登记表/overrides **缺 6 个 render 必需字段**，需 OPS 补录。⚠️ 注意 `ensure-search-volume` 已经跑过并写了 `C486="0"`，**所以这不是老的"空 search_volume→render park"那个坑**，别照旧结论下药。
  - **③ 值得治本的检查缺口**：`gg-seo-brief-preflight.mjs` 的 cluster-readiness 只校验「选题登记表有行 + cluster_id」，**不覆盖上述 6 个字段** → 133 个 PID 全报 `ready` 却每篇必 park，preflight 与"能否真的出稿"完全脱钩。第一层修复的效果只是把失败从 abort 前移到 park，产出仍是 0。建议把这 6 个字段纳入 preflight 的 fail-closed 校验（或让 render 缺字段时走 override 兜底）。
  - 处置：①②均属运营/写操作，GSC cron 未擅自动手；飞书已发（`om_x100b68de7648c0a8b1f8b5c90accdc0`，`ok:true`）。**补齐 ①②之前授稿产出仍为 0。**
  - **2026-08-17 复验：①② 仍【未修】，连续 4 天零动作，与 08-15/08-16 逐字相同。** 三项短路判据：plan mtime 仍 `08-13 18:58`（四天无人动）；err/out.log mtime 冻结 `08-13 19:00`、`duplicate` 计数冻结 **59（日增量 +0 = lane 压根没起跳，不是 lane 崩了）**；`launchctl list | grep gengrowth` 仍**无** `com.gengrowth.seo-blog`、也无 `com.gengrowth.gengrowth-author`（两个 plist 都在盘）。`tick complete` 仍停 `07-24 19:08`（**停摆第 24 天**）；`en_urls=361` 连续第 19 天持平（07-30→08-17）；GSC 候选=0、未开浏览器、0 提交。飞书第 11 次告警已发（`om_x100b671d36da9ca0b2c77bca1ec4753`，`ok:true`，累计 **11 次仅 08-04 一次引发动作**）。⚠️ 在有人执行下面两步之前，每日 cron 的结果都会逐字相同，**停摆天数本身是升级信号**。
  - **2026-08-15 复验：①② 均【仍未修】，连续 2 天零动作。** `launchctl list | grep gengrowth` 里**仍无** `com.gengrowth.seo-blog`（plist 在盘）；out/err.log mtime 仍停 `08-13 19:00`、`tick complete` 仍停 `07-24 19:08`（停摆第 22 天）；plan mtime 仍 `08-13 18:58`；`en_urls=361` 连续第 17 天持平。⚠️ 注意此时 err.log **日增量 = +0**，这**不是** 08-08 记的"lane 本身停了"那种故障，而是 lane 压根没加载（lane 活着被 preflight 卡时才是 +7/天）。飞书二次告警已发（`om_x100b68cb091584a4b49db863542c227`，`ok:true`，累计第 9 次仅 08-04 一次触发动作）。
- [x] 2026-08-13 19:01 | **✅ 授稿 lane 硬停已修复并验证活过来**（三步全做完，实跑验证）：① plan `~/gengrowth-ops/inbox-maboyang/06-tasks/tasks/2026-05-27-W22-blog-output-plan.md` 删掉重复块 —— 实测是 **514-516 三行被整块重复粘贴到 517-519**（不只是记录里说的"518 一行"），其中 KB-001/FH-001 是 `[x]` 不触发检查、只有 `PG-KB-002` 是 `[ ]` 所以只报它；已删整块避免以后有人把 `[x]` 改回 `[ ]` 再次触发。② `PG-VEDIC-009`/`PG-VEDIC-010` 改成**引用块 `>` 形式**（保留选题信息+恢复说明，不是删除）—— 因为补 sheet 行需要真实 cluster_id/关键词数据，属 OPS 活，不能编。**OPS 补登记后把这两行改回 `- [ ] ` 即可恢复**（⚠️别用 `* [ ]`/`- [~]`，preflight 会抛 `non-canonical unchecked row`）。③ 三个 plist **根因比记录的更深：不是"未加载"而是被 `launchctl disable` 显式禁用**（`launchctl print-disabled gui/$UID` 可见），所以单纯 `load`/`bootstrap` 会报 `Bootstrap failed: 5: Input/output error`；正确修法=**先 `launchctl enable gui/$UID/<label>` 再 `bootstrap`**，三个现已全部 LOADED。**验证**：写只读诊断脚本一次性扫全部 133 个 active PID（避免"修一个暴露一个"），确认无第三个坑（cluster_id 全 OK）；实跑 `gg-seo-brief-preflight.mjs --plan … --attested-manifest … --json` 返回 `{"mode":"cluster-readiness","status":"ready"}` exit 0；`launchctl kickstart` 后进程表出现 `gg-seo-autopilot.mjs --author --task PG-FA-001 --limit 1` = **真的在授稿了**。⚠️ 同时发现 `com.gengrowth.seo-author` / `com.gengrowth.seo-autopilot` 的 **plist 文件根本不存在**（授稿实际走 `com.gengrowth.seo-blog`，18:30 起每 30 分钟一 tick）；`flow-driver` 恢复后是 `GG_FLOW_DRIVER_APPLY=1` 有侧效模式（fix→重过门→可能 merge→部署 prod，上限 MAX_FIX=1/MAX_ARCHIVE=5），首跑在 load 后 2 小时。**待观察**：prod `en_urls` 是否从 361 开始回升。
- [x] ~~2026-08-13 | **🚨 授稿 lane 硬停已 20 天~~（已由上条取代）（08-13 继续用短路判据：plan mtime 仍是 08-05 17:57 = **八天**无人再动 + err.log 尾部错误文本仍是 `duplicate active page ids`，两项均未变 → 根因链未变，直接复用结论；累计 **58 次**（较 08-12 的 51 次 **+7**，即昨日 7 次 tick 全 abort → lane 活着但每次卡 preflight）；`tick complete` 仍停在 07-24 19:08；prod `en_urls` 冻在 361 已 **15 天** 07-30→08-13）—— 下面三步一步都没做，飞书告警累计 **8 次**（08-04 / 08-06 / 08-07 / 08-08 / 08-10 / 08-11 / 08-12 / 08-13，08-13=`om_x100b68e15b0b210cb395be7c1759074`）只有 08-04 那次引发动作（**1/8**）。✅ 08-09 记的"告警通道被权限分类器拦死"已证实是**瞬时**误伤：08-10、08-11 连续两轮同一条 `node tools/scripts/gg-notify.mjs raw --text "…"` 都直接 `ok:true`（08-11=`om_x100b689ea66f38a4b24cf4ee7883103`）→ **不需要改 settings 加 allow 规则**，被拦时下一轮 cron 重试即可。**以下为 08-06 记录的完整根因链，仍然逐字有效：08-05 17:57 有人编辑了 plan `~/gengrowth-ops/inbox-maboyang/06-tasks/tasks/2026-05-27-W22-blog-output-plan.md`，但编辑引入了**完全相同的两行** `- [ ] \`PG-KB-002\` Jungkook birth chart`（第 **515** 行和第 **518** 行）→ preflight 的 duplicate 检查排在 missing-row 检查**之前**，所以报错从 `PG-VEDIC-009: missing existing row`（累计 56 次）换成了 `pinned plan contains duplicate active page ids`，lane 依旧 0 产出（out.log 最后一次 `tick complete` 仍是 **07-24 19:08**，08-05 全天 7 次 tick 全 abort）。**修复顺序（两步都要做，只做第一步会立刻退回旧错）**：① 删掉 plan **第 518 行**（与 515 重复）→ 解除 duplicate；② 随后 `PG-VEDIC-009` 的 missing-row 会重新暴露——plan 第 **678/679** 行的 `PG-VEDIC-009`(Selena Gomez zodiac sign)/`PG-VEDIC-010`(Ariana Grande zodiac sign) 在 canonical 选题登记表**仍未登记**（已复验：VEDIC 只有 008 在 row361，009/010 不存在；KB-002 本身在 row358 正常）→ 要么补两行并**填 cluster_id**（只补行不填会换成 `missing cluster_id` 继续卡），要么把 plan 那两行摘掉。③ 保活仍未修：`com.gengrowth.lane-watchdog` / `com.gengrowth.flow-driver` / `com.gengrowth.ledger-reconcile` 三个 plist 在 `~/Library/LaunchAgents/` 但**仍全部未加载**，需 `launchctl load`。后果：prod `en_urls` 冻在 **361**（截至 08-13 已 15 天），GSC cron 每天候选=0 不是因为都收录了。（取代下方 08-04 同主题条目，根因链已更新）
- [x] 2026-08-04（已由 08-13 修复条取代）| **🚨 授稿 lane 已硬停 11 天（07-24 至今 0 产出）— 需运营 + launchctl 两处人工修复**：`com.gengrowth.seo-blog` 每天 7 次 tick 全部 abort 在 `active brief preflight failed: PG-VEDIC-009: missing existing row in 选题登记表`（err.log 里 49 次同一行），最后一次真正跑完是 **07-24 19:08**。根因=plan `~/gengrowth-ops/inbox-maboyang/06-tasks/tasks/2026-05-27-W22-blog-output-plan.md` 第 671/672 行的待做项 `PG-VEDIC-009`(Selena Gomez zodiac sign)/`PG-VEDIC-010`(Ariana Grande zodiac sign) 在 canonical 选题登记表**没有对应行**（VEDIC 只到 007/008），而 `gg-seo-brief-preflight.mjs:133` 是 fail-closed，一个坏 PID 卡住全部授稿。**修复①（运营）**：给选题登记表补 009/010 两行并**填 cluster_id**（只补行不填 cluster_id 会换成 `missing cluster_id` 继续卡），或把 plan 那两行摘掉。**修复②（保活）**：`com.gengrowth.lane-watchdog` / `com.gengrowth.flow-driver` / `com.gengrowth.ledger-reconcile` 三个 plist 在 `~/Library/LaunchAgents/` 但**全部未加载**（flow-driver 日志停在 07-08），所以这 11 天静默死亡无人报警 → 需 `launchctl load` 回来。后果：prod `en_urls` 冻在 361 已 6 天，GSC cron 每天候选=0 但不是因为都收录了。
- [ ] 2026-07-04 | **CELEB-025(tobey-maguire) sheet 脏关键词根因修复（防复发）**：选题登记表 CELEB-025 行的 assoc_keywords 列(B) 混入了幻觉关键词 `emma watson zodiac sign`（brief-template artifact，[[celeb-batch-schema-and-worker-footguns]]）→ gate 的 review[schema] FAIL。我已从 _staging 草稿 strip 掉该词 + retry-failed 让 025 重入门（这次上线不受阻），但**根在 sheet**：不修 sheet 的话下一批 celeb 又会重犯。需去 canonical workbook 选题登记表把该行 assoc_keywords 里的 `emma watson zodiac sign` 删掉。（Phase 6 auto-retry 不会自动修这个——它是真内容错，正确判 permanent 留人工。）
- [ ] 2026-07-04 | **阶段 5+6 已 LAND（此条仅记状态，无需动作）**：flow 可靠性全量修复 5 阶段全完成——阶段4 回填事务(95c7960d)+阶段5 lane watchdog(deae5a6d)+阶段6 transient park 自动重试(831151cf) 均已 land+启用+对抗评审。9 lane 全载=safe full-auto 闭环。之前"CELEB-026 待 retry / 阶段5 watchdog"两条待办已被 6 的 auto-retry + 5 的 land 取代，可视为完成。
- [ ] 2026-07-03 | **CELEB-026(pen-lope-cruz-birth-chart)待 --retry-failed 重 gate**：7/3 nightly 期间 claude/codex 撞订阅 session-limit(21:30 重置)，它已授稿+推 preview 但**评审 worker exit1**(配额致)→ needs_human(stage=pushed-preview)。配额已恢复，跑 `node tools/scripts/gg-seo-autopilot.mjs --retry-failed --branch seo/auto/2026-07-03-PG-CELEB-026 --evidence "quota reset, re-gate"` 重跑 gate(codex+chrome，~10min，会自动 verify→merge→prod)。其余 4 篇 authoring 假 park(WC-042/CELEB-025/027/028)已 retry-author 清+重授(作者 lane 会自动重写，WC-042 已手动 kick)。
- [ ] 2026-07-03 | **阶段 5 watchdog(lane 保活，杀静默死亡)**：cron 已于 7/3 晚重启(7 lane 全载)，但**尚无 watchdog**——lane 若 launchd 未加载/静默死亡无人知(这正是审计 [[flow-reliability-audit-0703]] 的核心病)。阶段 4(回填对账)已 LAND(95c7960d)。下一步做 lanes-manifest + 心跳/lane_stale 告警(lib/flow-state.mjs 已预留 heartbeat)。过渡期(lane 无 watchdog 裸奔)可接受但应尽快补。
- [ ] 2026-07-04+ | **GSC 补提（astrologywiki 已再次追平 + gengrowth glossary 部分待续）——从 结果复盘表 底部往上**：**【2026-08-03 完成 · 候选=0 · 0 提交 · 未开浏览器 · ⚠️发布链路连续 5 天零新增】**结果复盘表候选=**0**（申请时间空 & day14_收录≠Y 的行为零）→ 按 SOP **未开浏览器、未发飞书**。sync-published en_urls=**361**（appended=0，与 07-30/07-31/08-01 完全持平）、sync-recap en_rows=**312**（持平，updated=289 skipped=23）、sync-request-queue rows=**4**（P1=4，全是已提交待收录项，非新候选）→ **发布链路自 07-30 起连续 5 天零新增文章**，这才是"候选恒为 0"的真因（不是都收录完了，是根本没有新文章上线）。**★ GitHub 侧已解封/已换号（本轮按 08-01 的建议复验）**：`gh api user` 返回 **`phananhson733-oss`**（不再是被封的 `xdawayer`），`gh auth status` = 已登录、token scopes `gist/read:org/repo/workflow`；`~/oracle` 的 `git ls-remote origin HEAD` **可达**（HEAD=`c33b2cdb`，而本地 clone 仍停在 07-10 的 `7624bf9`）→ [[github-account-suspended-0728]] 的 remote 不可达阻塞**已解除**（换成了另一个 GitHub 账号）。**所以"5 天无新文章"不能再归因于 GitHub 封停**，需另查授稿/发布 lane 是否在跑（建议下一轮或人工查 `com.gengrowth.seo-author` / `com.gengrowth.seo-autopilot` launchd lane 是否 loaded、ledger 里是否堆积 needs_human）。本轮无 GSC 操作、无配额消耗。**【2026-08-02 缺失】**该日无记录（cron 未跑或未回填）。**【2026-08-01 完成 · 候选=0 · 0 提交 · 未开浏览器】**结果复盘表候选=**0**（申请时间空 & day14_收录≠Y 的行为零）→ 按 SOP **未开浏览器、未发飞书**。**昨日的隐患已自解**：`enfp-gemini`(r309) 的 `day14_收录` 已翻成 `Y`/索引修复状态=已收录 → 不再每日重复列为候选，无需人工干预 index-monitor 的收录检测。07-30 提交的那 9 篇（r308–r316）**day14_收录 现已全部翻 Y=已收录**，本轮抽查 r311–r316 全为 `2026-07-30 / Y / 已收录`。**仍无新文章上线**：sync-published en_urls=**361**（07-30/07-31 持平，appended=0）、sync-recap en_rows=**312**（持平，updated=291）→ 发布链路连续 3 天零新增，**建议下一轮复验 `gh api user`**（[[github-account-suspended-0728]] 的封停是否仍在，是否卡住 merge/build）。sync-request-queue rows=**4**（P1=4，较 07-31 的 13 下降，均为已提交待收录项，非新候选）。**【2026-07-31 完成 · 唯一候选已收录 · 0 提交】**结果复盘表候选=1，且就是昨日已确认收录的 `enfp-gemini`(r309)。本轮 GSC 复查仍报「网址已收录到 Google / 网页已编入索引」→ 按 SOP 跳过省配额，**0 提交、未发飞书**（避噪声）。**无新文章上线**：sync-published en_urls=**361**（与 07-30 持平，appended=0）、sync-recap en_rows=**312**（与 07-30 持平）→ 说明发布链路当日无新增（GitHub 封停影响待观察，本轮未复验 `gh api user`）。sync-request-queue(rows=13,P1=4,P3=9) 已同步。Claude-in-Chrome 扩展连接正常（`Browser 1`, isLocal=true），双击模式一次进框。**注**：`enfp-gemini` 的表 day14_收录 列仍未翻，明日 cron 还会把它列为唯一候选——若连续多日如此，可考虑让 index-monitor 的收录检测把它翻成已收录，否则每天都要白开一次浏览器。**【2026-07-30 完成 · 提交 9 篇 · 配额充足 · venus-in-taurus 软404 自愈】**结果复盘表候选=10，curl 批量预检 **10/10 全过**（全在 sitemap、静态 stub 专属 `<title>`、正文 25–28KB，无软404）。Claude-in-Chrome 扩展本轮已恢复连接（`list_connected_browsers` 返回 Browser 1，昨日的阻塞解除）。**提交 9 篇**（GSC 均为「已发现-尚未编入索引」健康态 → 已请求编入索引；已回填 申请时间=2026-07-30 + 状态=已重新提交，index-tracking 9/9 + 结果复盘表 9/9，sync-recap + sync-request-queue 已同步）：`wanda-maximoff-zodiac-sign`(recap r316) / `thor-zodiac-sign`(r315) / `seventeen-zodiac-signs`(r314) / `marvel-characters-zodiac-signs`(r313) / `ive-members-zodiac-signs`(r312) / `intp-zodiac-sign`(r311) / `esfp-zodiac-sign`(r310) / `bts-compatibility-zodiac`(r308) / `venus-in-taurus`(r292)。**★ venus-in-taurus 软404 终于自愈并提交成功**——07-25 首判软404，连续跳过 5 轮（07-25/26/27/28）+ 07-29 因扩展未连未提，本轮 GSC 状态已转「已发现-尚未编入索引」、请求被接受。印证 07-29 记的那条"软404 自愈可以很慢（5 天+），别据此人工干预"。**已收录跳过 1**（省配额）：`enfp-gemini`(r309)，GSC 报「网址已收录到 Google / 网页已编入索引」，表 day14_收录 列滞后未翻属正常，明日 cron 仍会把它列为候选、再查再跳即可。本轮无配额错误（未探到上限）。飞书已推。**★浏览器操作要点补充**：提交成功 toast 后再点搜索框，用 `click → wait 2s → click → type` 的**双击模式**可 100% 一次进框（比单纯 wait 3s 更稳，本轮 9 篇仅第 2 篇失败一次，改双击后再无失败）。**注**：sync-published en_urls=361（07-29 为 336）、recap en_rows=312（07-29 为 303）→ 新文章仍在正常上线，说明 GitHub 封停对发布链路的影响已解除或已绕过（未专门复验 `gh api user`）。**【2026-07-29 阻塞 · 唯一候选已自愈可提 · Chrome 未连接→未提交】**结果复盘表候选=1（`venus-in-taurus` r292）。**curl 预检本轮首次全过 → 软404 已自愈（历时 5 天：07-25→07-29，远超 messi-vs-yamal 的 ~2 天先例）**：静态 stub `<title>`=专属标题 "Venus in Taurus Brings Love Back to What Lasts"（非通用标题）、body 25,750 bytes（健康区间 24–30KB）、`sitemap.xml` grep 命中 `<loc>https://www.astrologywiki.com/en/wiki/venus-in-taurus</loc>` → 按 SOP 属**必提**候选。**但无法提交：本机 Claude-in-Chrome 扩展未连接**——`list_connected_browsers` 连续 2 次返回 `[]`、`tabs_context_mcp` 连续 2 次返回"Claude in Chrome is not connected"（Chrome 进程本身在跑，PID 515，150.0.7871.186；即扩展未装/未登录/未与本 app 同账号，非 Chrome 没开）。按 SOP 如实报告未提交、未盲试其他浏览器（in-app Browser 无 GSC 登录态）。**人工动作**：在 Chrome 打开 Claude 侧边栏并用与本 app 相同账号登录（扩展 https://chromewebstore.google.com/detail/fcoeoabgfenejglbffodgkkbkcdhcgfn），恢复后次日 cron 会自动重试这 1 篇；或手动在 GSC 提 `https://www.astrologywiki.com/en/wiki/venus-in-taurus` 并回填 申请时间。本轮 0 提交 → 未发飞书。sync-published(en=336,appended=0)/sync-recap(en_rows=303,updated=273)/sync-request-queue(rows=15,P1=4,P3=11) 已跑。**【2026-07-28 完成 · 提交 10 篇 · 配额充足 · ⚠️发现 GitHub 账号封停】**astrologywiki 提交 **10 篇**（GSC 均为「已发现-尚未编入索引」健康态 → 已请求编入索引；提交前 curl 批量预检：10 篇全在 sitemap、静态 stub 渲染专属标题、无软404；已回填 申请时间=2026-07-28 + 状态=已重新提交，index-tracking/结果复盘表 各 10/10，sync-recap + sync-request-queue 已同步）：`suga-bts-birth-chart`(recap r307) / `severus-snape-zodiac-sign`(r306) / `selena-gomez-birth-chart`(r305) / `rm-bts-birth-chart`(r304) / `rihanna-birth-chart`(r303) / `jisoo-birth-chart`(r302) / `harry-potter-characters-zodiac-signs`(r301) / `dumbledore-zodiac-sign`(r300) / `bts-members-zodiac-signs`(r299) / `blackpink-zodiac-signs`(r298)。**跳过 1 篇软404**：`venus-in-taurus`(r292) —— 已连续 **4 天**未自愈（sitemap grep=0、live `<title>` 仍为通用 "Free Birth Chart Calculator"、`/api/wiki/items` 404），按 SOP 未盲提。本轮无已收录跳过项、无配额错误。飞书已推。**🚨 本轮意外发现（高优先级，需人工处理）**：**GitHub 账号 `xdawayer` 已被封停** —— `git fetch` / `git ls-remote origin` / `gh api user` 全部返回 `Your account is suspended`(403)，flow-mvp 与 oracle 两个仓库的 remote 都不可达。这会**卡死 autopilot 的 PR 发布门 / merge / Vercel 部署链路**（gg-preview-gate 依赖 `gh pr diff`/`gh pr merge`）。需人工向 GitHub Support 申诉恢复。注：`venus-in-taurus` 迟迟不进部署 bundle 很可能与此同源（新文章无法 push/merge → 无新 build）；但 r298–r307 这 10 篇已 live，说明封停发生在它们上线之后，时间点待查。本地 oracle clone 停在 7624bf9(07-10) 且无法 fetch，故无法从 git 侧验证 venus-in-taurus 是否真进过 `data/articles/index.ts`。**【2026-07-27 完成 · 唯一候选软404(连续3天未愈) · 未提交】**结果复盘表候选=1（`venus-in-taurus` r292），仍是 07-25 跳过的那篇软404。开浏览器前 curl 预检：live `<title>`=通用 "Free Birth Chart Calculator"（非专属标题）、`/api/wiki/items/venus-in-taurus?lang=en|zh` 均返 **404**、sitemap grep=**0** → 三项均证仍未进部署 bundle（软404 未自愈）→ 按 SOP **未开浏览器、未盲提**，本轮 0 提交、未发飞书。sync-published(en=254,appended=0)/sync-recap(en_rows=293,updated=274)/sync-request-queue(rows=5) 已跑。**⚠️跟进**：该篇软404 已连续 **3 天**未自愈（07-25→07-26→07-27），超过 messi-vs-yamal 的 ~2 天自愈先例——若明日 cron 仍未愈，可能不是普通部署传播延迟，值得查 oracle 该 slug 是否真进了 `data/articles/index.ts` / 是否需触发一次 build 重新部署（参照 [[wiki-item-fetch-stale-bundle]]）。**【2026-07-26 完成 · 唯一候选软404 · 未提交】**结果复盘表候选=1（`venus-in-taurus` r292），正是 07-25 跳过的那篇软404。开浏览器前按 SOP 做廉价 curl 预检：live `<title>` 仍为通用 "Free Birth Chart Calculator"（非专属标题）、`/api/wiki/items/venus-in-taurus?lang=en` 返 **404**、sitemap grep=**0** → 三项均证文章仍未进部署 bundle（软404 未自愈，才隔一天；参照 messi-vs-yamal 需 ~2 天）→ 按 SOP **未开浏览器、未盲提**（盲提会触发 GSC live 实测→软404 拒收=白费一次），等下次 build 部署自愈、次日 cron 再试。本轮 0 提交 → 未发飞书（避噪声）。sync-published(en=254,appended=0)/sync-recap(en_rows=293,updated=269)/sync-request-queue(rows=10) 已跑。**【2026-07-25 完成 · 候选清零 · 配额充足】**astrologywiki 提交 **5 篇**（GSC 均为「已发现-尚未编入索引」健康态 → 已请求编入索引；提交前 curl 批量预检 live 渲染专属标题、无软404；已回填 申请时间=2026-07-25 + 状态=已重新提交，sync-recap/sync-request-queue 已同步，剩余候选=0 除软404 那篇）：`victor-wembanyama-zodiac-sign`(recap r297) / `taylor-swift-travis-kelce-synastry`(r296) / `shohei-ohtani-birth-chart`(r295) / `robert-downey-jr-birth-chart`(r294) / `jalen-brunson-birth-chart`(r293)。**跳过 1 篇软404**：`venus-in-taurus`(r292)——GSC 报「Google 无法识别此网址 / 未检测到任何引荐站点地图」，开 live 页面确认渲染 **"Failed to fetch wiki item"**（通用标题、`/api/wiki/items` 404，未进部署 bundle=部署传播延迟），按 SOP 未盲提，待下次 build 自愈、次日 cron 重试。本轮无已收录跳过项、无配额错误（未探到上限）。飞书已推。**【2026-07-24 完成 · 无新候选 · 未提交】**结果复盘表候选=0（申请时间空 & 未收录的行为零，昨日 7 篇已清空全部积压）→ 按 SOP 未开浏览器、未发飞书。sync-published(en=335,appended=0)/sync-recap(en_rows=287,updated=262) 已跑。**【2026-07-23 完成 · 候选清零 · 配额充足】**astrologywiki 提交 **7 篇**（GSC 均为「已发现-尚未编入索引」健康态 → 已请求编入索引；提交前批量 curl 预检：7 篇全在 sitemap 内、live 渲染专属标题且无 "Failed to fetch"、无软404；已回填 申请时间=2026-07-23 + 状态=已重新提交，sync-recap/sync-request-queue 已同步，剩余候选=0）：`saturn-return-career-change`(r291) / `scorpio-mbti-type`(r290) / `jennifer-lopez-birth-chart`(r289) / `taylor-swift-travis-kelce-wedding-astrology`(r288) / `mars-in-aries`(r287) / `jang-wonyoung-birth-chart`(r286) / `argentina-vs-spain-world-cup-final-astrology-2026`(r285)。本轮无已收录跳过项、无配额错误（未探到上限）。**★浏览器操作要点**：提交成功 toast 关闭后**必须等 ~3s 再点搜索框**，否则首次 click+type 一定不进框（本轮前 4 篇每篇都白费一次往返，加 3s wait 后 100% 一次进框）。飞书已推（另有一条"通知自检"噪声消息，可忽略）。**【2026-07-22 完成 · 候选清零 · 配额充足】**astrologywiki 提交 **5 篇**（GSC 均为「已发现-尚未编入索引」健康态 → 已请求编入索引；提交前逐条 curl 校验 live 渲染正常＋均在 sitemap 内；已回填 申请时间=2026-07-22 + 状态=已重新提交，sync-recap/sync-request-queue 已同步，剩余候选=0）：`what-is-my-theme-for-this-year`(r284) / `the-most-common-mbti-types-for-each-zodiac-sign`(r283) / `leandro-paredes-birth-chart`(r279，昨日撞配额顺延) / `am-i-meant-to-be-my-own-boss`(r278，顺延) / `messi-vs-yamal-astrology`(r277)。**★ messi-vs-yamal 软404 已自愈验证 SOP**：07-20 该篇因未进部署 bundle 被 live 实测判软404 拒收，本轮 GSC 状态已变「已发现-尚未编入索引」、live 页面渲染正常 → 提交成功，印证"软404 跳过、等下次 build 自愈、次日 cron 重试"的处置正确。本轮无已收录跳过项、无配额错误（未探到上限）。飞书已推（另有一条"通知自检"噪声消息，可忽略）。**【2026-07-21 完成 · 配额早耗】**astrologywiki 提交 **3 篇**（均 GSC「已发现-尚未编入索引」健康态→已请求编入索引，已回填申请时间=2026-07-21+状态=已重新提交，sync-recap/sync-request-queue 已同步）：`surviving-saturn-return` / `mars-in-scorpio` / `ferran-torres-birth-chart`。**配额在第 4 篇撞「超出每日配额」**（`leandro-paredes-birth-chart`，点请求即弹"今天已超出每日配额"）→按 SOP 停手未硬试。今日配额比常态 ~10-12 低（疑因 07-20 提的 8 篇仍在账号级 ~24h 滚动窗口内）。**顺延明日 cron（3 篇候选）**：`leandro-paredes-birth-chart`(r279) / `am-i-meant-to-be-my-own-boss`(r278) / `messi-vs-yamal-astrology`(r277，上轮软404 现已自愈，静态 HTML 16-18K 正文、无 "Failed to fetch"、live 渲染正常)。跳过 1：`kylian-mbappe-birth-chart`(r220，canonical 收口重复页不提)。飞书已推。**【2026-07-20 完成 + 软404 渲染门发现】**astrologywiki 提交 **8 条**（均 GSC live 测试通过→已请求编入索引，配额充足未耗尽，均回填申请时间=2026-07-20、状态=已重新提交、sync 同步、候选清零）：`saturn-return-in-taurus` / `jupiter-in-cancer` / `saturn-return-in-aries` / `moon-in-cancer` / `moon-in-capricorn` / `france-england-3rd-place-match-astrology-prediction` / `how-do-i-stop-burnout` / `tom-brady-birth-chart`。**跳过 2**：① `messi-vs-yamal-astrology`(r277，最新一篇)——GSC 报"Google 无法识别此网址"，点请求后 live 测试**拒收=软404**；查 live 页面渲染 **"Failed to fetch wiki item"**（`/api/wiki/items/<slug>` 404，文章未进部署 bundle=部署传播延迟，非页面坏，待下次 build 自愈，明日 cron 会重试）；② `kylian-mbappe-birth-chart`(r220，canonical 收口重复页不提)。**★SOP 精化**：候选里"Google 无法识别此网址"状态**不能盲提**，须先开 live 页面看是否渲染正常（专属标题+正文=OK→live 测试通过被接受，如 moon-in-capricorn/france-england；通用"Free Birth Chart Calculator"标题+"Failed to fetch"=软404→提了必被拒，如 messi-vs-yamal）。**【2026-07-18 完成】**astrologywiki 提交 **9 条**（均 GSC 确认未收录→已请求编入索引，配额充足未耗尽，均回填申请时间=2026-07-18、状态=已重新提交、sync 已同步）：`venus-in-gemini` / `saturn-return-in-cancer` / `saturn-return-age-27` / `am-i-on-the-right-path` / `birth-chart-interpretation` / `elvis-birth-chart` / `what-do-i-need-to-let-go-of` / `what-is-my-love-language` / `how-long-does-saturn-return-last`。**已收录跳过 4**（省配额，GSC 已确认收录、表 day14_收录 列滞后未翻属正常）：`brad-pitt-birth-chart` / `caitlin-clark-birth-chart` / `saturn-return-in-cancer-meaning` / `saturn-return-age-30`。`kylian-mbappe-birth-chart`(r220) 仍按 canonical 收口跳过不提。飞书已推。**【2026-07-09 完成 + sitemap 漏收根因结案】**astrologywiki 提交 **6 条**（配额充足无耗尽，均回填申请时间=2026-07-09、状态=已重新提交、sync 已同步、候选清零）：`kai-cenat-zodiac-sign` / `egypt-world-cup-2026-astrology` / `alexander-zverev` / `arthur-fery` / `anne-hathaway` / `kevin-de-bruyne`。gengrowth 本轮无待提（07-07 已清）。**★ 07-07 那条"sitemap 漏收待查"已查清并修复（/investigate）**：① 表面"漏收"根因=**build 时 sitemap 的部署传播延迟**（发文→Vercel 部署重建 public/sitemap.xml 之间有滞后，GSC 在窗口内检查就报"未检测到引荐站点地图"；非生成器 bug，diogo-jota 等现已在 sitemap）；② 真 bug=**slugify 掉重音**（`[^a-z0-9]` 没先 NFKD 归一化 → "Mbappé"→"kylian-mbapp"截断 + 与旧 `mbappe-birth-chart` 重复）——已在 flow-mvp **5 处 slugify 统一加 `.normalize('NFKD').replace(/\p{M}+/gu,'')`+回归测试**（kebabSlug，已在 origin/main，防今后所有带重音名字）；③ 重复的 `kylian-mbapp`/`mbappe` 两篇做 **canonical 收口**（新→旧 canonicalPath + sitemap:false + stub/runtime 一致），已合并 oracle main（commit 04a1ed3，含 generate-seo-pages stub-canonical 补全 + article-seo-config 回归），**Vercel 部署中，prod 边缘缓存刷新后生效**（待复验 prod kylian canonical→mbappe + 移出 sitemap）。**【2026-07-07 17:35 完成】**配额窗口已滑出（昨日 11 条过 ~25h），本波共提交 **9 条无耗尽**。① **astrologywiki 7 条全提+回填**（申请时间=2026-07-07、状态=已重新提交，sync-recap/sync-request-queue 已同步，候选清零、结果复盘表已全部追平）：`rayan-cherki`(r212)/`kylian-mbapp`(r211)/`achraf-hakimi`(r210)【17:35 新增】+ `england-vs-norway`(r209)/`jessica-pegula`(r208)/`cole-palmer`(r207)/`erling-haaland-girlfriend`(r206)。② **gengrowth 2 条**：`/en/glossary/attribution-model` + `/en/glossary/robots-txt`（英文，均"已抓取-尚未编入索引"→已请求；仍**不在 gengrowth index-tracking**，无法回填结果复盘表，需修 /en/blog/ 解析缺口后补记账）。⚠️ **待查（sitemap 漏收）**：`rayan-cherki`/`kylian-mbapp`/`achraf-hakimi`/`erling-haaland-girlfriend` 4 个 `-birth-chart` slug 在 GSC 报"Google 无法识别此网址/未检测到任何引荐站点地图"，疑似未进 astrologywiki sitemap（同 07-06 `diogo-jota`）；而 `england-vs-norway`/`jessica-pegula`/`cole-palmer` 已在 sitemap。建议查 sitemap 生成是否漏收 `-birth-chart` slug。**【2026-07-07 12:20 状态·已被上条取代】**首次试探即"超出了配额"（昨仅过 ~20h），当日 0 可提，按 SOP 停下未硬试。**【2026-07-06 自动补提结果】本波共提交 11 条，配额在第 12 次耗尽（账号级 ~10-12/天，与实测一致）。① astrologywiki 7/7 全部提交并回填（申请时间=2026-07-06、状态=已重新提交、结果复盘表+index-tracking 已同步 sync-recap/sync-request-queue）：malia-obama / lebron-james / john-denver / angela-nikolau / diogo-jota / travis-kelce / tobey-maguire。② gengrowth glossary 提交 4 个：topical-authority / zero-click-search / referral-program / ai-citation（均"已抓取-尚未编入索引"）；zh/glossary/robots-txt 查得**已收录**→永久跳过。③ 配额耗尽未提，留下次窗口：`gengrowth /en/glossary/attribution-model`（"已抓取-尚未编入索引"，已确认未收录，只差配额）。⚠️ gengrowth 这 4+1 glossary 页仍**不在 gengrowth index-tracking**（工具 /en/blog/ 解析缺口）→ 无法回填结果复盘表，需那个 session 修解析缺口后补记账。⚠️ 另注 `diogo-jota-birth-chart` 在 GSC 显示"Google 无法识别此网址 / 未检测到任何引荐站点地图"——疑似未进 astrologywiki sitemap，虽已请求编入索引但值得查 sitemap 是否漏收该 slug。—— 以下为原 SOP（不变）—— ⚠️配额机制（7/2-7/3 实测）：手动 Request Indexing 配额是**账号级/~24h 滚动窗口**，不按站独立、不午夜清零；续提前先拿最新一条试探，"超出配额"就等窗口滑出别硬试。**astrologywiki 已全部追平**：7/3 提交 9 篇（jaylen-brown/coco-gauff/vera-wang/bella-hadid/elliot-page/thylane-blondeau/maya-joint/morocco/teyana-taylor，均已回填申请时间=2026-07-03、状态=已重新提交），之前 7/2 已提 6 篇。以后 astrologywiki 新文章上线后，仍从 结果复盘表 底部（最新）往上、每条查未收录才提、提完回填。**剩 gengrowth glossary/use-case 页**（`/en/glossary/topical-authority` 已确认未收录待提；zh/robots-txt、zero-click-search、referral-program、robots-txt、ai-citation、attribution-model）——注意 gengrowth glossary 页**不在 gengrowth 的 index-tracking 里**（工具 /en/blog/ 解析缺口），提了也回填不进 结果复盘表，需那个 session 修解析缺口后才能记账。—— 原 SOP（不变）——⚠️SOP 纠正（wzb 7/2 指出）：**提交 worklist 的唯一来源是 canonical workbook 的「结果复盘表」，从最下面一行往上（底部=最新），不是 sitemap lastmod**（我 7/2 错用 sitemap，漏了比 ben-shelton 更新的 maya-joint/thylane-blondeau/elliot-page/bella-hadid）。**次日待提清单（结果复盘表 申请时间为空 & 未收录，底部→上）**：`bella-hadid-birth-chart`(r193)、`elliot-page-birth-chart`(r192)、`thylane-blondeau-birth-chart`(r191)、`maya-joint-birth-chart`(r190)、`morocco-world-cup-2026-astrology`(r184)、`teyana-taylor-birth-chart`(r182)、`jannik-sinner-zodiac-sign`(r181，表状态滞后实为已收录、查了会跳过)、`serena-williams-birth-chart`(r180)（均 `https://www.astrologywiki.com/en/wiki/<slug>`）。**每条流程**：GSC 网址检查→**先确认"未收录"才点请求编入索引**（绝不盲点，省配额，每站约 7-10/天）→ 提交成功后**必须回填**：见下"回填"。7/2 已提交 6 篇（cardi-b/carlos-alcaraz/pride-month-astrology/total-solar-eclipse-2026/usa-pluto-return-astrology/ben-shelton，已回填申请时间=2026-07-02、状态=已重新提交）。已收录跳过：matheus-cunha/mo-salah/prince-william/jannik-sinner。重定向跳过：solar-return-chart（GSC报"网页会自动重定向"，sitemap规范URL却重定向=内容/配置问题，值得查）。gengrowth 独立配额，剩 8 个 glossary/use-case 页续做（`/en/glossary/topical-authority`、`/zh/glossary/robots-txt`、`/en/glossary/zero-click-search`、`/en/glossary/referral-program`、`/en/glossary/robots-txt`、`/en/glossary/ai-citation`、`/en/glossary/attribution-model`；`/glossary/referral-program` 无locale疑似重定向）。**回填（提交后必做，否则表看不出已申请）**：结果复盘表工具没有"标记UI已提交"的动作（`--sync-recap` 把「申请时间」永远置空、只有 process-fixed 才填），所以用 scratch 脚本 import `gg-index-monitor.mjs` 的 helper（`readTrackingRows/updateTrackingRow`+`readRecapRows/updateRecapRow`，canonical workbook）：① index-tracking 设 `resubmitted_at=日期`+`fix_status=已重新提交`（这样 sync 不冲状态、且从 request-queue 移除）② 结果复盘表 设「申请时间」=日期+「索引修复状态」=已重新提交（mergeRecapRow 会保留申请时间但会用 fresh 覆盖状态，故状态靠①的 fix_status 撑）。模板见 scratchpad/stamp-submitted.mjs。**GSC搜索框聚焦不稳**：点击后先打字再截图确认文本进框再回车（约半数尝试没进）；提交成功 toast 会盖框、别马上再输入。**收尾两步（每波必做，wzb 定）**：飞书群推送提交条数（`gg-lark-notify.sh`）+ 同步 sheet。注：Indexing API 对普通 blog 不合规（官方仅 JobPosting/BroadcastEvent），改走手动 UI。
- [ ] 2026-06-21 | **重启 autopilot 前先确认 Mac-mini(awayer_mini)codex CLI 已装+已认证**：发布门 codex 事实审已改为**必过门**(commit 见 [[project_autopilot_codex_gate]]),`gg-autopilot-preflight.mjs` 现在把 codex 当必需依赖——缺失/未认证时整条 fire 会大声中止并 @PM@OPS(这是设计的安全姿态,不是 bug)。重启前:在 awayer_mini 上 `command -v codex || ls ~/.npm-global/bin/codex ~/.local/bin/codex`,并跑一次 `codex exec -s read-only -c model=gpt-5.5 - <<<'say OK'` 确认能认证返回;plist 已把 `GG_CODEX_BIN` 指向 `gg-codex-pr-review.mjs`。紧急要发无 codex 时才设 `GG_CODEX_GATE_REQUIRED=0`(回退 best-effort,会重新放行事实错,慎用)。注:plist 同时去掉了自相矛盾的 `LimitLoadToSessionType=Background`——重装 plist 时用 `launchctl bootstrap gui/$(id -u)` 加载(保留用户钥匙串)。
- [ ] 2026-06-21 | **(跟进)autopilot 发布门 codex 审的 TOCTOU 加固**：`/review` 第三轮发现的 pre-existing 缺口(见 [[project_autopilot_codex_gate]])——codex 在 `gg-preview-gate.mjs` step-4b(T1)审 `gh pr diff`,但 `--merge` pin 的是 `gg-seo-autopilot.mjs:1279`(T2)才抓的 `headRefOid`,且该抓取 best-effort(失败→unpinned merge fail-open)。T1→T2 间 force-push 会让 PASS 不绑定实际合并的 commit。串行 tick+PID mutex 使概率低,非阻塞,但应跟进:codex 绑定到单一 SHA + required 模式下 `headRefOid` 缺失即 park(强制 pin)。非本次 codex-gate 改动引入,故未在该 PR 一并改。
- [ ] 2026-06-21 | **GSC 补提 scotland-brazil ZH**：6/21 批次 GSC「请求编入索引」做到第 10 个时当日配额(~10-12/天)用尽，`https://www.astrologywiki.com/zh/wiki/scotland-brazil-world-cup-astrology` 未提交（其余 5 EN + 4 ZH 已提交）。次日经 Chrome MCP 在 GSC（已登录 sc-domain:astrologywiki.com）补提这 1 个即可。它已在 sitemap，不补也会被自然抓取。GSC 搜索框聚焦：中性点击 + double_click + 等 2s 出下拉再输入；绝不在未确认 URL 前点请求按钮（会重复提交浪费配额）。
- [ ] 2026-06-18 | **gengrowth-publish cron 依赖 SUPABASE_ACCESS_TOKEN**：`~/.config/gg/_gg.env` 里的 `SUPABASE_ACCESS_TOKEN`（sbp_，personal access token，从 `supabase login` 的 keychain 提出）驱动 Lane A 取 service_role 写 gengrowth.ai blog_posts。它长效但可被撤销/轮换。失效后 cron 记 "SB_KEY unavailable" + Feishu 告警 + 跳过（fail-safe，不崩）。重配：`supabase login` 后 `security find-generic-password -s "Supabase CLI" -w` 取新值更新 _gg.env，或 dashboard/account/tokens 重新生成。
- [ ] 2026-06-10 | **autopilot 配图依赖 Google 会话保活**：cron 自动配图（hero）需本机 Google/Gemini 登录态，testing-mode cookie 会过期。过期后新文章只发"文字+内联图"并标 `needs_hero`（不阻塞发布）。会话过期时重跑 `node tools/scripts/gg-gemini-cookie-import.mjs`（需在 Chrome 已登录 Google + 钥匙串授权一次），或 `cd ~/.openclaw/workspace/skills/baoyu-danger-gemini-web/scripts && bun main.ts --login`。
- [ ] 2026-06-10 | **验证首篇真实 cron 自动配图**：配图闭环已在 scratch worktree 端到端验过，但尚未经过真实无人值守 tick（W22 队列已 drained）。下批选题（W23）加入、autopilot 发出首篇带图文章后，线上验收 + **人眼看一眼 hero 质量**（QA 的中线接缝检测只是 backstop，非替代人眼；diptych prompt 约束是第一道防线）。
- [ ] 2026-06-10 | **定期清 `needs_hero` 积压**：会话中断期间发布的文章会标 `needs_hero`（claims 里）。目前无自动补图 lane，需人工或重跑补 hero。会话恢复后查 `grep -l needs_hero` autopilot claims 并补图。
- [ ] 2026-06-10 | **（知情，可选）** cron 配图默认开启（`GG_AUTOPILOT_ILLUSTRATE` 未设=开）。若想先观察几篇再全量放开，可在 plist/env 临时设 `GG_AUTOPILOT_ILLUSTRATE=0`（纯文字）或 `GG_ILLUSTRATE_LLM_PLAN=0`（只模板 hero、不出内联）。
- [ ] 2026-06-10 | **（待 wzb 定夺）** cron 配图 provider 维持 gemini-web（零成本、有 ToS 风险）。若要切官方 keyed API（gpt-image-1），设 `GG_GEMINI_SKILL` 指向 baoyu-imagine + 填 OpenAI key，不改码。

## 已完成

- [x] 2026-06-18 | **两条 publish-only cron 拉起来**：Lane A `com.gengrowth.gengrowth-publish`（每小时，扫 ready 草稿→bridge `--emit rest` 幂等 upsert 到 gengrowth.ai blog_posts，已 live 的跳过）；Lane B `com.gengrowth.seo-autopilot`（25min，`GG_AUTOPILOT_MODE=publish-only` 永不撰写，`latestPlan` 排除 gengrowth plan，publish-gate 套 gtimeout，driver 级硬闸）。均加载在 gui/Aqua 域（去掉 LimitLoadToSessionType=Background）。Codex 复核 + 15/15 smoke。
- [x] 2026-06-18 | gengrowth.ai 发布桥 `gg-md-to-gengrowth-blog.mjs`（md→sanitize-html→blog_posts，pillar=seo_content，`--emit rest/sql`）；supabase CLI 流程通畅（2.106）。
- [x] 2026-06-10 | astrologywiki.com 剩余 89 篇 blog 全量配 hero+内联图并上线 prod（89/89 验收通过，commit ca8edde）。
- [x] 2026-06-10 | 配图并入 autopilot cron 闭环（`lib/illustrate.mjs` + `gg-hero-qa.mjs`，三未决项已决策，端到端验证通过）。
