---
title: 跨会话待办提醒
type: reminders
updated: 2026-06-18
---

# 跨会话待办提醒（Reminders）

> 会话开始时若有 `- [ ]` 未完成条目，在第一条回复前列出（见 AGENTS.md §七）。

## 待完成

- [ ] 2026-07-03 | **CELEB-026(pen-lope-cruz-birth-chart)待 --retry-failed 重 gate**：7/3 nightly 期间 claude/codex 撞订阅 session-limit(21:30 重置)，它已授稿+推 preview 但**评审 worker exit1**(配额致)→ needs_human(stage=pushed-preview)。配额已恢复，跑 `node tools/scripts/gg-seo-autopilot.mjs --retry-failed --branch seo/auto/2026-07-03-PG-CELEB-026 --evidence "quota reset, re-gate"` 重跑 gate(codex+chrome，~10min，会自动 verify→merge→prod)。其余 4 篇 authoring 假 park(WC-042/CELEB-025/027/028)已 retry-author 清+重授(作者 lane 会自动重写，WC-042 已手动 kick)。
- [ ] 2026-07-03 | **阶段 5 watchdog(lane 保活，杀静默死亡)**：cron 已于 7/3 晚重启(7 lane 全载)，但**尚无 watchdog**——lane 若 launchd 未加载/静默死亡无人知(这正是审计 [[flow-reliability-audit-0703]] 的核心病)。阶段 4(回填对账)已 LAND(95c7960d)。下一步做 lanes-manifest + 心跳/lane_stale 告警(lib/flow-state.mjs 已预留 heartbeat)。过渡期(lane 无 watchdog 裸奔)可接受但应尽快补。
- [ ] 2026-07-04+ | **GSC 补提 gengrowth glossary 页（astrologywiki 已追平）——从 结果复盘表 底部往上**：⚠️配额机制（7/2-7/3 实测）：手动 Request Indexing 配额是**账号级/~24h 滚动窗口**，不按站独立、不午夜清零；续提前先拿最新一条试探，"超出配额"就等窗口滑出别硬试。**astrologywiki 已全部追平**：7/3 提交 9 篇（jaylen-brown/coco-gauff/vera-wang/bella-hadid/elliot-page/thylane-blondeau/maya-joint/morocco/teyana-taylor，均已回填申请时间=2026-07-03、状态=已重新提交），之前 7/2 已提 6 篇。以后 astrologywiki 新文章上线后，仍从 结果复盘表 底部（最新）往上、每条查未收录才提、提完回填。**剩 gengrowth glossary/use-case 页**（`/en/glossary/topical-authority` 已确认未收录待提；zh/robots-txt、zero-click-search、referral-program、robots-txt、ai-citation、attribution-model）——注意 gengrowth glossary 页**不在 gengrowth 的 index-tracking 里**（工具 /en/blog/ 解析缺口），提了也回填不进 结果复盘表，需那个 session 修解析缺口后才能记账。—— 原 SOP（不变）——⚠️SOP 纠正（wzb 7/2 指出）：**提交 worklist 的唯一来源是 canonical workbook 的「结果复盘表」，从最下面一行往上（底部=最新），不是 sitemap lastmod**（我 7/2 错用 sitemap，漏了比 ben-shelton 更新的 maya-joint/thylane-blondeau/elliot-page/bella-hadid）。**次日待提清单（结果复盘表 申请时间为空 & 未收录，底部→上）**：`bella-hadid-birth-chart`(r193)、`elliot-page-birth-chart`(r192)、`thylane-blondeau-birth-chart`(r191)、`maya-joint-birth-chart`(r190)、`morocco-world-cup-2026-astrology`(r184)、`teyana-taylor-birth-chart`(r182)、`jannik-sinner-zodiac-sign`(r181，表状态滞后实为已收录、查了会跳过)、`serena-williams-birth-chart`(r180)（均 `https://www.astrologywiki.com/en/wiki/<slug>`）。**每条流程**：GSC 网址检查→**先确认"未收录"才点请求编入索引**（绝不盲点，省配额，每站约 7-10/天）→ 提交成功后**必须回填**：见下"回填"。7/2 已提交 6 篇（cardi-b/carlos-alcaraz/pride-month-astrology/total-solar-eclipse-2026/usa-pluto-return-astrology/ben-shelton，已回填申请时间=2026-07-02、状态=已重新提交）。已收录跳过：matheus-cunha/mo-salah/prince-william/jannik-sinner。重定向跳过：solar-return-chart（GSC报"网页会自动重定向"，sitemap规范URL却重定向=内容/配置问题，值得查）。gengrowth 独立配额，剩 8 个 glossary/use-case 页续做（`/en/glossary/topical-authority`、`/zh/glossary/robots-txt`、`/en/glossary/zero-click-search`、`/en/glossary/referral-program`、`/en/glossary/robots-txt`、`/en/glossary/ai-citation`、`/en/glossary/attribution-model`；`/glossary/referral-program` 无locale疑似重定向）。**回填（提交后必做，否则表看不出已申请）**：结果复盘表工具没有"标记UI已提交"的动作（`--sync-recap` 把「申请时间」永远置空、只有 process-fixed 才填），所以用 scratch 脚本 import `gg-index-monitor.mjs` 的 helper（`readTrackingRows/updateTrackingRow`+`readRecapRows/updateRecapRow`，canonical workbook）：① index-tracking 设 `resubmitted_at=日期`+`fix_status=已重新提交`（这样 sync 不冲状态、且从 request-queue 移除）② 结果复盘表 设「申请时间」=日期+「索引修复状态」=已重新提交（mergeRecapRow 会保留申请时间但会用 fresh 覆盖状态，故状态靠①的 fix_status 撑）。模板见 scratchpad/stamp-submitted.mjs。**GSC搜索框聚焦不稳**：点击后先打字再截图确认文本进框再回车（约半数尝试没进）；提交成功 toast 会盖框、别马上再输入。**收尾两步（每波必做，wzb 定）**：飞书群推送提交条数（`gg-lark-notify.sh`）+ 同步 sheet。注：Indexing API 对普通 blog 不合规（官方仅 JobPosting/BroadcastEvent），改走手动 UI。
- [ ] 2026-06-21 | **重启 autopilot 前先确认 Mac-mini(awayer_mini)codex CLI 已装+已认证**：发布门 codex 事实审已改为**必过门**(commit 见 [[project_autopilot_codex_gate]]),`gg-autopilot-preflight.mjs` 现在把 codex 当必需依赖——缺失/未认证时整条 fire 会大声中止并 @PM@OPS(这是设计的安全姿态,不是 bug)。重启前:在 awayer_mini 上 `command -v codex || ls ~/.npm-global/bin/codex ~/.local/bin/codex`,并跑一次 `codex exec -s read-only -c model=gpt-5.5 - <<<'say OK'` 确认能认证返回;plist 已把 `GG_CODEX_BIN` 指向 `gg-codex-pr-review.mjs`。紧急要发无 codex 时才设 `GG_CODEX_GATE_REQUIRED=0`(回退 best-effort,会重新放行事实错,慎用)。注:plist 同时去掉了自相矛盾的 `LimitLoadToSessionType=Background`——重装 plist 时用 `launchctl bootstrap gui/$(id -u)` 加载(保留用户钥匙串)。
- [ ] 2026-06-21 | **(跟进)autopilot 发布门 codex 审的 TOCTOU 加固**：`/review` 第三轮发现的 pre-existing 缺口(见 [[project_autopilot_codex_gate]])——codex 在 `gg-preview-gate.mjs` step-4b(T1)审 `gh pr diff`,但 `--merge` pin 的是 `gg-seo-autopilot.mjs:1279`(T2)才抓的 `headRefOid`,且该抓取 best-effort(失败→unpinned merge fail-open)。T1→T2 间 force-push 会让 PASS 不绑定实际合并的 commit。串行 tick+PID mutex 使概率低,非阻塞,但应跟进:codex 绑定到单一 SHA + required 模式下 `headRefOid` 缺失即 park(强制 pin)。非本次 codex-gate 改动引入,故未在该 PR 一并改。
- [ ] 2026-06-21 | **GSC 补提 scotland-brazil ZH**：6/21 批次 GSC「请求编入索引」做到第 10 个时当日配额(~10-12/天)用尽，`https://www.astrologywiki.com/zh/wiki/scotland-brazil-world-cup-astrology` 未提交（其余 5 EN + 4 ZH 已提交）。次日经 Chrome MCP 在 GSC（已登录 sc-domain:astrologywiki.com）补提这 1 个即可。它已在 sitemap，不补也会被自然抓取。GSC 搜索框聚焦：中性点击 + double_click + 等 2s 出下拉再输入；绝不在未确认 URL 前点请求按钮（会重复提交浪费配额）。
- [ ] 2026-06-18 | **gengrowth-publish cron 依赖 SUPABASE_ACCESS_TOKEN**：`~/.config/gg/_gg.env` 里的 `SUPABASE_ACCESS_TOKEN`（sbp_，personal access token，从 `supabase login` 的 keychain 提出）驱动 Lane A 取 service_role 写 gengrowth.ai blog_posts。它长效但可被撤销/轮换。失效后 cron 记 "SB_KEY unavailable" + Feishu 告警 + 跳过（fail-safe，不崩）。重配：`supabase login` 后 `security find-generic-password -s "Supabase CLI" -w` 取新值更新 _gg.env，或 dashboard/account/tokens 重新生成。
- [ ] 2026-06-10 | **autopilot 配图依赖 Google 会话保活**：cron 自动配图（hero）需本机 Google/Gemini 登录态，testing-mode cookie 会过期。过期后新文章只发"文字+内联图"并标 `needs_hero`（不阻塞发布）。会话过期时重跑 `node tools/scripts/gg-gemini-cookie-import.mjs`（需在 Chrome 已登录 Google + 钥匙串授权一次），或 `cd ~/.openclaw/workspace/skills/baoyu-danger-gemini-web/scripts && bun main.ts --login`。
- [ ] 2026-06-10 | **验证首篇真实 cron 自动配图**：配图闭环已在 scratch worktree 端到端验过，但尚未经过真实无人值守 tick（W22 队列已 drained）。下批选题（W23）加入、autopilot 发出首篇带图文章后，线上验收 + **人眼看一眼 hero 质量**（QA 的中线接缝检测只是 backstop，非替代人眼；diptych prompt 约束是第一道防线）。
- [ ] 2026-06-10 | **定期清 `needs_hero` 积压**：会话中断期间发布的文章会标 `needs_hero`（claims 里）。目前无自动补图 lane，需人工或重跑补 hero。会话恢复后查 `grep -l needs_hero` autopilot claims 并补图。
- [ ] 2026-06-10 | **（知情，可选）** cron 配图默认开启（`GG_AUTOPILOT_ILLUSTRATE` 未设=开）。若想先观察几篇再全量放开，可在 plist/env 临时设 `GG_AUTOPILOT_ILLUSTRATE=0`（纯文字）或 `GG_ILLUSTRATE_LLM_PLAN=0`（只模板 hero、不出内联）。
- [ ] 2026-06-10 | **（待 wzb 定夺）** cron 配图 provider 维持 gemini-web（零成本、有 ToS 风险）。若要切官方 keyed API（gpt-image-1），设 `GG_GEMINI_SKILL` 指向 baoyu-imagine + 填 OpenAI key，不改码。

## 已完成

- [x] 2026-06-18 | **两条 publish-only cron 拉起来**：Lane A `com.gengrowth.gengrowth-publish`（每小时，扫 ready 草稿→bridge `--emit rest` 幂等 upsert 到 gengrowth.ai blog_posts，已 live 的跳过）；Lane B `com.gengrowth.seo-autopilot`（25min，`GG_AUTOPILOT_MODE=publish-only` 永不撰写，`latestPlan` 排除 gengrowth plan，publish-gate 套 gtimeout，driver 级硬闸）。均加载在 gui/Aqua 域（去掉 LimitLoadToSessionType=Background）。Codex 复核 + 15/15 smoke。
- [x] 2026-06-18 | gengrowth.ai 发布桥 `gg-md-to-gengrowth-blog.mjs`（md→sanitize-html→blog_posts，pillar=seo_content，`--emit rest/sql`）；supabase CLI 流程通畅（2.106）。
- [x] 2026-06-10 | astrologywiki.com 剩余 89 篇 blog 全量配 hero+内联图并上线 prod（89/89 验收通过，commit ca8edde）。
- [x] 2026-06-10 | 配图并入 autopilot cron 闭环（`lib/illustrate.mjs` + `gg-hero-qa.mjs`，三未决项已决策，端到端验证通过）。
